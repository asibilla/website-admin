import { styled } from '@mui/material/styles';

export const EditArticleTextInputContainer = styled('div')(({ theme }) => ({
  marginBottom: theme.spacing(2),
  width: '100%',
}));

export const EditArticleSelectContainer = styled('div')(({ theme }) => ({
  marginBottom: theme.spacing(2),
  width: '100%',
}));
