version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 24
    commands:
      - yarn install
  build:
    commands:
      - yarn build
  post_build:
    commands:
      - aws s3 sync out/ s3://$S3_BUCKET --delete
